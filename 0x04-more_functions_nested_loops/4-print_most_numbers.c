#include"main.h"
/**
*main - prints out numbers 
*Return: Always 0
*/

void print_most_numbers(void)
{

	int x;
for (x = 0; x <= 9; x++)
	{
	if (z != 2 && z != 4)
	_putchar (z + '0');
	_putchar ('\n');
	}
return;
}
